package com.example.pharmazione.utils;

import android.view.View;
import com.example.pharmazione.persistance.Document;

public interface ItemClickListener {
     void onClick(Document item, View sliderLayout);
}
